var React = require('react');
var ReactDOM = require('react-dom');
var LeadCapture = require('./components/LeadCapture.jsx');

const main = document.getElementById('main');

ReactDOM.render(<LeadCapture />, main);